package com.codesimples.jdbc

import com.codesimples.objectives.persistence.adapter.user.TransactionAdapter

class JDBCTransactionAdapter extends TransactionAdapter {
  override def commit() = {

  }

  override def rollback() = {

  }
}
